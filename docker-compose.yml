services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: abbey
      POSTGRES_PASSWORD: abbey_dev_pw
      POSTGRES_DB: abbey_circle
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: